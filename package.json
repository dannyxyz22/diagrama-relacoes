{
  "name": "app-grafos-relacoes",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Webapp para gerar diagramas de relações (atores/nós e relações/arestas coloridas com rótulos), com layout automático e nós arrastáveis.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "cytoscape": "^3.30.2",
    "cytoscape-fcose": "^2.2.0",
    "cytoscape-svg": "^0.4.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.45.0",
    "vite": "^8.1.4",
    "wrangler": "^4.111.0"
  }
}
